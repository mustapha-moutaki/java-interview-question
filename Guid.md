# Citi Karat Interview Preparation Guide

# Table of Contents

1. Java Core Questions
2. Object-Oriented Programming
3. Java Collections
4. Multithreading & Concurrency
5. Exception Handling
6. JVM / JDK / JRE
7. Spring Boot Questions
8. Spring Security
9. Hibernate & JPA
10. REST API Questions
11. SQL & Database Questions
12. Microservices
13. System Design
14. Design Patterns
15. Coding Patterns
16. Top Coding Problems
17. Behavioral Questions
18. HR Questions
19. Final Interview Tips

---

# 1. Java Core Questions

## Java Basics

1. What is Java?
```text
java it's object oriented programming language that runs on any platform without any modification, it used to develop web applications, mobile applications and more, one of it's main advantages is that it runs everywhere 
```
2. Why is Java platform independent?
```text
java it's independent because it runs on jvm, and jvm is installed on every platform
```
3. What is JVM?
```text
jvm it's stands for java virtual machine, and it's a proogram in the jre that runs the java code bycode and it's responnsable also for garbage collection and it's who make java platform independent.
```
4. What is JDK?
```text
jdk it's stands for java development kit and it's a software that contains all tools needed to develop java application like jvm, jre and compiler and debuger.

5. What is JRE?
```text
jre stands for java runtime environment and it's the respponsable for running java applications, and it contains all the necessery tools to run java code like jvm and libraries

```
6. Difference between JDK, JRE, and JVM?
```text
jvm is repsonsable to transform the code into bycode and execute it 
menwhile jre is environment where jvm runs and contains all needed libaries necessery to run java code
finally iijdk or java development kit it's reposnable for java development it' contains jvm and jre and garbade collector and compiler and debugger.
```
7. What is bytecode?
```text
bycode it's a code intermediate between java code and jvm it's generated by the compiler to execute it in jvm.
``` 
8. What is class loading?
```text
class loading it's a process when jvm load the class into memory in the runntime using classLoader
```
9. What are access modifiers?
```text
access modifiers it's used to control the visibility of data ;
- public: the data is accessible from all classes
- private: the data is accessible only for the same class.
- protected: the data is accessible only for the same class or inherited class child
- default: the data is accessible only for the same class or in the same package
```
10. Difference between public, private, protected, and default?
```text
- public: the data is accessible from all classes
- private: the data is accessible only for the same class.
- protected: the data is accessible only for the same class or inherited class child
- default: the data is accessible only for the same class or in the same package
```
11. What is a constructor?
```text
constructor:it's a special method that initialize object, and it's automatically called when the object is created.
```
```java
public class Car {
    String brand;
    String model;

    // Constructor
    public Car(String brand, String model) {
        this.brand = brand;
        this.model = model;
    }
}

```
12. Difference between constructor and method?
```text
constructor: it's a special method that used to initiialize the object, it's called automatically when the object created and it has the same name of the class.
meanWhile method: is a block of code that runs when the method called and it can have any name, also it should have return type or void
```
13. Can constructor be private?
```text
yes constructor can be private and it's used to avoid creating object from outside the class. and it's use in the singleton design pattern.
```
```java
public class Singleton{
    private Singleton(){
    }
    private static Singleton instance;

    public static Singleton getInstance(){
        if(instance == null){
            instance = new Singleton();
        }
        return instance;
    }
}
```

14. What is method overloading?
```text
Overlaoding method: it is when we have mutiple methods have the same name but the number of parameters or type of return paramters is different 
```

```java
// example:
add(int a, int b)

add(int a, int b, int c)

add(double a, double b)
```
15. What is method overriding?
```text
method overriding: it's when the same method name with same parameters used by the parent class implemented by the child class.
```
```java
// example:
class Vehicle {
    public void drive() {
        System.out.println("Vehicle is driving");
    }
}

class Car extends Vehicle {
    // the child class override the method of the parent class -> drive() 
    // by using @override annotation.
    @Override
    public void drive() {
        System.out.println("Car is driving");
    }
}
```
16. Difference between overloading and overriding?
```text
Overloading: it's when we have mutiple methods have the same name but the number of parameters or type of return paramters is different 
mean while overriding: it's when the same method name with same parameters used by the parent class implemented by the child class
```
17. What is static keyword?
```text
static keyword: it's used to define that this method or variable is belong t the class not the object, we use it to save the data in one copy and accessible by all object of the class
```
```java
class Car {

    static int counter = 0;

    Car() {
        counter++;
    }
}
// if we create
new Car();
new Car();
new Car();
// then
counter will be 3 not 1 1 1 it will be shared between all objects
```


18. What is final keyword?
```text
The final keyword is used to restrict modification.

A final variable cannot be reassigned
A final method cannot be overridden
A final class cannot be inherited
```

19. What is finally block?
```text
finally block it's a block that runs in every cases even the exception is thrown or not
we use it to close the resources like db connection.
it used after ttry and catch blocks
```

20. What is finalize()?
```text
finalize it's a method that is called by the garbage collector before destroying the object, it was used for cleanup, but now it's deprecated and not recommended anymore.
```

21. Difference between final, finally, and finalize?
```text
final is a keyword used to restrict modification.

final variable → cannot be reassigned
final method → cannot be overridden
final class → cannot be inherited

finally is a block that always executes whether an exception happens or not. It is mainly used for resource cleanup.

finalize() is a method called by the garbage collector before destroying an object. It was used for cleanup, but it is deprecated and not recommended anymore.

```

22. What is this keyword?
```text
this keyword used to refer to the current object.
```

23. What is super keyword?
```text
super keyword used to refer to the parent class object.
```

24. What is encapsulation?
```text
encapsulation is a mechanism of wrapping data and the methods that operate on the data in a single unit called a class. 
```

25. What is inheritance?
```text
inheritance is a mechanism of creating a new class from an existing class, which is called parent class or super class
```

26. What is polymorphism?
```text
polymorphism is a oop concept that means when the same method can have different behavior
```
27. What is abstraction?
```text
abstraction is a oop concept when we hide the unnecessery details from the user and show only the necessary details
```
28. Difference between abstract class and interface?
```text
Abstract class:
- Can have abstract and non-abstract methods
- Can have constructors
- Can have instance variables
- A class can extend only one abstract class

Interface:
- Can have only abstract methods (before Java 8)
- Can have default and static methods (Java 8+)
- Cannot have constructors
- Cannot have instance variables (only constants)
- A class can implement multiple interfaces
```
29. What is interface?
```text
An interface is a contract that defines behaviors that implementing classes must provide. It mainly contains method declarations, and after Java 8 it can also contain default and static methods.
```

30. Can interfaces have methods?
```text
yes, interface can have abstract methods and concret methods after java 8 and also static and private methods
```

31. What are default methods?
```text
it's a method that can have body in the interface and any class that implement the interface can inherit this method wihout overriding it except if the class wanna to customize it's implementation.
```
32. What are functional interfaces?
```text
A functional interface is an interface that contains only one abstract method. 
```
```java
@FunctionalInterface
interface MyInterface {
    void myMethod(); // Single abstract method
}
```
33. What is lambda expression?
```text
A lambda expression is a short block of code that takes parameters and returns a value. Lambda expressions provide a concise way to implement functional interfaces.
```
```java
// Without lambda expression
MyInterface myInterface = new MyInterface() {
    @Override
    public void myMethod() {
        System.out.println("Hello World");
    }
};

// With lambda expression
MyInterface myInterface = () -> System.out.println("Hello World");
```
34. What is Stream API?
```text
Stream API is a powerful feature in java to process and manipulate collections of data;
```
35. Difference between == and equals?
```text
== is a operator that it used to compare between two values and references 
equals it's a method that it usedd to compare betweeen two variables values.

```
36. Difference between String, StringBuilder, and StringBuffer?
```text
-  String is immutable, meaning its value cannot be changed after creation.

- StringBuilder is mutable and not thread-safe, but it is faster.

- StringBuffer is mutable and thread-safe, but slower because it uses synchronization.
```
37. Why String is immutable?
```text
String is immutable mainly for security and performance reasons.
```

38. What is garbage collection?
```text
Garbage collection is a process or mechanism that is used by java automatically to remove unused objects from the memory for better performance and less resources consumption.
```
39. How garbage collector works?
```text
The garbage collector automatically detects objects that are no longer referenced and removes them from heap memory to free space. Sometimes the JVM pauses application threads temporarily during garbage collection, which is called “Stop The World”.
```

40. What is memory leak?
```text
ut's when the unused objects are still referenced so the garbage collector cannot remove them from the memory.
```

41. Stack memory vs Heap memory?
```text
 - Stack memory is used for method calls, local variables, and execution flow. It is faster and temporary.

 - Heap memory is used to store objects and instance data. It is shared between threads and managed by the garbage collector.
```
42. Primitive types in Java?
``` text
primitive data types: are the java basic data types(8 types) like
- int
- float
- double
- boolean
- char
- byte
- short
- long

and non primitive data types:
- String
- Array
- Class
- Object
```
43. Wrapper classes?
```text
Wrapper classes are the class version of the primitive types. for example "Integer" is the wrapper class for "int"
```
44. Autoboxing and unboxing?
```text
Autoboxing : primitive to wrapper class

Unboxing is the automatic conversion of wrapper classes to their primitive types.
```
45. What is type casting?
```text
casting is process of converting one data type to another type.

Casting types:
- implicit casting (from small type to large type)

- explicit casting (from large type to small type)
Example:

int num2 = 10;
long long = num2; // implicit casting

double doubleNumber = 20.5;
int numbeer = (int) doubleNumber; // explicit casting


```
46. Difference between checked and unchecked exceptions?
```text
Checked expception: those exceptions that check in compile time and needs to handle them using try and catch or throw
like: IOException, SQLException

Unchecked exception: are those exceptions that are checked in runtime and don't need to handle them using try and catch or throw
like: ArithmeticException, NullPointerException

```
47. What is serialization?
```text
it's process of converting an object to bytes so it can be stored in the file or memory.
```
```java
// Example:

class User implements Serializable {

}
```
48. What is deserialization?
```text
it's a process of converting bytes to object.
```
49. transient keyword?
```text
The transient keyword is used to prevent a variable from being serialized.
in other words it's used to indicate that a variable should not be stored in the file or memory.
```

50. volatile keyword?
```text
The volatile keyword iis used to indicate that a varibale changed by a thread so it should be immediately visible to other threads.
it's read data from memory not form cache.
```

---

# 2. Object-Oriented Programming

1. Explain encapsulation with example.
```text
it's oop concept to hide data using access modifiers and controll access using getters and setters methods.
```
```java
class User{
    private String name;
    private int age;

    public String getName() {
        return name;
    }
    public void setName(String name) {
        this.name = name;
    }
    public int getAge() {
        return age;
    }
    public void setAge(int age) {
        this.age = age;
    }
}
```
2. Explain inheritance with example.
```text
it's oop concept to inherit properties and methods from class to anther class
```
```java
class Animal{
    void eat(){
        system.out.println("the animal is eating");
    }
}

class Dog extends Animal{
    void bark(){
        system.out.println("the dog is barking");
    }
}
```
3. Explain polymorphism with example.
```text
polymorphism is oop concept to perform one action in many forms.
```
```java
class Animal {
    void sound() {
        System.out.println("Animal sound");
    }
}

class Dog extends Animal {
    @Override
    void sound() {
        System.out.println("Bark");
    }
}

class Cat extends Animal {
    @Override
    void sound() {
        System.out.println("Meow");
    }
} 
```
4. Explain abstraction with example.
```text
abstraction is oop oncept to hide the complexity of the class from the users
```
```java
abstract class Animal{
    abstract void sound();
}

class Dog extends Animal{
    void sound(){
        System.out.println("Bark");
    }
}
```

5. What is composition?
```text
is a has-a relationShip between two classes, meaninng one class is a part of another class and can be reused in other classes.
```
```java
example:
class engine{
    void start(){
        system.out.println("the engine is starting");
    }
}
class car{
    engine engine;
    car(){
        this.engine = new engine();
    }
}
```

6. Composition vs inheritance?
```text
composition: is a has-a relationShip between two classes, meaninng one class can't live independenlty from the other class.
meanwhile inheritance : it's a class created dependently from another class.
```

7. What is aggregation?
```text
aggregation is a weak relationShip between two classes, meaninng class can live independently from the other class
```
```java
// Exampple:
class Student{

}
class University{
    List<Student>students;
}
```

8. What is association?
```text
association it's a general relationShip between two classes that are related to each other but each class can live independenlty from the other class
```

```java
//Exampple:
class Person{
    String name;
}
class Address{
    String address;
}

class PersonAddress{
    Person person;
    Address address;
}
```

9. What is tight coupling?

```text
tight coupling is a relationShip between two classes that are dependent on each other
```
```java
// Exampple:
class Database{
    void save(){
        system.out.println("data saved to database");
    }
}
class UserController{
    Database db;
    userController(){
        this.db = new Database();
    }
}
```
10. What is loose coupling?
```text
loose coupling is a relationShip between two classes that are independent on each other
```

```java
// Exampple:
class Database{
    void save(){
        system.out.println("data saved to database");
    }
}
class UserController{
    private Database db;
    userController(Database db){
        this.db = db;
    }
}
```

11. What are SOLID principles?
```text
solid principle are 5 principle that help us to write a good maintable and scalable oriented object code.
```

12. Explain Single Responsibility Principle.
```text
Single responsability principle: is one method can have only one duty.
```

13. Explain Open Closed Principle.
```text
Open close principle: the class shoud be able for extend but closed for modification.
```

14. Explain Liskov Substitution Principle.
```text
linkov substitution principle: the child parent class shoud be able to replace wihtout breaking the code.
```

15. Explain Interface Segregation Principle.
```text
interface segregation principle: the class shoudnot be forced to implement methods it doesn't need or use.
```

16. Explain Dependency Inversion Principle.
```text
the high level class should not depend on the low level class, both should depend on abstraction.
```

17. What is dependency injection?
```text
dependency injection is a design pattern that is used to provide the dependencies of a class to it.
```
18. Constructor injection vs field injection?
```text
- constructor injection is more recommended because it's mutable and easy for test.
- field injection is not recommended because it's immutable and hard for test.
```

19. Why constructor injection is preferred?
```text
constructor injection is more recommended because it's immutable and easy for test.
```

20. What is inversion of control?
```text
inversion of control is a design pattern that allow the framework to control the flow of the application not the developer.
```

# 3. Java Collections

1. What is Collection Framework?
```text
collection framework is a set of classes and interfaces to store a group of objects.
it's prvide:
- list
- map
- set
- queue
and implementes:
- arrayList
- linkedList
- stack
- HashMap
```

2. Difference between List and Set?

```text
list: it store data based on the insertion order and it allows duplicated values.
set: it store data based on the natural order and it rejects duplicated values.
```

3. Difference between ArrayList and LinkedList?

```text
arraylist: it's fast for getting data and slow for insetion and it's not thread safe.
linnkedList: it's slow for getting data and fast for insertion and it's not thread safe.
```

4. Difference between HashMap and Hashtable?

```text
hashmap: it's fast for getting data and slow for insetion and it's not thread safe.
hashtable: it's fast for getting data and slow for insertion and it's thread safe.
```

5. Difference between HashMap and TreeMap?
```text
hashmap : it's fast for getting data and slow for insetion and it's not thread safe.
treemap: it's slow for getting data and fast for insertion and it's not thread safe, and it's ordered. it use red-black tree  internally.
```

6. Difference between HashSet and TreeSet?
```text
hashset: it's fast for getting data and slow for insetion and it's not thread safe.
treeset: it's slow for getting data and fast for insertion and it's not thread safe, and it's ordered. it use red-black tree  internally.
```

7. What is PriorityQueue?
```text
priorityqueue: it's a queue that stores elements based on priority order.
the smallest/highest priority element is processed first.
```

8. What is Queue interface?
```text
queue interface: it's a queue that stores elements based on FIFO order.
```

9. What is Deque?
```text
double ended queue
it allow to insert and delete elements from both ends
```

10. How HashMap works internally?
```text
Hashmap it collection of key-value pairs, internally it use array of linked list and each place called bucket. by default hashmap has 15 buckets and load factor is 0.75 and it' snot thread safe and it's fast for getting data. it allow only one null key but it allows multiple null values
```

11. What is hash collision?
```text
colliision is when two objects keys generate same hashCode so they are stored in the same bucket.
```

12. What is load factor?
```text
load factor: is value that determine when hashmap resize. by default it's 0.75
and resizing  when size > capacity * load factor
```

13. What is hashing?
```text
hashing is a proccess to convert a key into hash code.
```

14. What is Comparable?
```text
comparable is an interface that is used to compare two objects.
```

15. What is Comparator?
```text
comparator is an interface that is used to compare two objects.
```

16. Difference between fail-fast and fail-safe iterator?
```text
fail-fast: it's an iterator that throw exception when the collection is modified during iteration.
fail-safe: it's an iterator that doesn't throw exception when the collection is modified during iteration.
```
17. What is ConcurrentHashMap?
```text
concurrentHashMap: it's a thread-safe hashmap.
```

18. Time complexity of HashMap operations?
```text
time complexity of hashmap operations is O(1).
```

19. Time complexity of ArrayList insertion?
```text
time complexity of arraylist insertion is O(1).
```

20. Time complexity of LinkedList insertion?
```text
time complexity of linkedlist insertion is O(1).
```

21. What is immutable collection?
```text
immutable collection: is a collection that cannot be modified.
```

22. What is Collections utility class?
```text
collections utility class: it's a utility class that provides methods to work with collections.
```

23. Difference between Arrays.asList and List.of?
```text
- Arrays.asList: it's a method that returns a list from an array.
- List.of: it's a method that returns a list from a varargs.
```

24. What is Iterator?
```text
iterator: it's an interface that is used to iterate over a collection.
```

25. What is ListIterator?
```text
list iterator: it's an interface that is used to iterate over a collection and it allows to add and remove elements from the collection.
```
---

# 4. Multithreading & Concurrency

1. What is thread?
```text
thread: it's a lightweight process that can run independently of other threads.
```
2. Difference between process and thread?
```text
process: it's a heavy weight process that can run independently of other processes.
thread: it's a lightweight process that can run independently of other threads.
```
3. How to create thread in Java?
4. Runnable vs Thread?
5. What is synchronization?
6. What is race condition?
7. What is deadlock?
8. What is starvation?
9. What is livelock?
10. What is thread safety?
11. What is ExecutorService?
12. What is Future?
13. What is CompletableFuture?
14. What is thread pool?
15. What is volatile keyword?
16. synchronized method vs synchronized block?
17. What is ReentrantLock?
18. What is wait()?
19. What is notify()?
20. What is notifyAll()?
21. Difference between sleep() and wait()?
22. What is atomic class?
23. What is CountDownLatch?
24. What is Semaphore?
25. What is cyclic barrier?

---

# 5. Exception Handling

1. What is exception?
2. Difference between error and exception?
3. Checked vs unchecked exceptions?
4. What is try-catch block?
5. What is finally block?
6. Can finally not execute?
7. What is throw keyword?
8. What is throws keyword?
9. Custom exceptions?
10. What is exception propagation?
11. Multiple catch blocks?
12. What is try-with-resources?
13. What is stack trace?
14. Best practices for exception handling?
15. Why avoid empty catch blocks?

---

# 6. JVM / JDK / JRE

1. Explain JVM architecture.
2. What is classloader?
3. Types of classloaders?
4. What is heap memory?
5. What is stack memory?
6. What is metaspace?
7. What is garbage collector?
8. Types of garbage collectors?
9. What is stop-the-world?
10. What is memory leak in Java?
11. What is OutOfMemoryError?
12. What is StackOverflowError?
13. What is JIT compiler?
14. How Java executes code?
15. What is bytecode verification?

---

# 7. Spring Boot Questions

1. What is Spring Boot?
```text
is a java framework build on the top of the spring framework and it's used to create java web applications.

``` 
2. Advantages of Spring Boot?
```text
- auto-configuration
- embedded server liek tommact
- no xml configuration
- spring starter
``` 
3. What is dependency injection?
```text
it's a desing pattern to  innject the dependencies in a class. and in spring boot we can inject the dependencies in 4 ways
1. using @Autowired annotation
2. using Constructor injection
3. using the setter injection
4. using the lombok annotation
```
4. What is IoC container?
```text
it's a spring boot framework that is used to manage injection of dependencies automatically without manual implementation.
```

5. What is Spring Bean?
```text
it's an object that runs when the spring boot applications starts
and it's managed by the ioc container.
``` 

6. What is @Component?
```text
it's an annotation that is used to mark a class as a spring bean.
``` 

7. What is @Service?
```text
it's an annotation that is used to mark a class as a been but it's used in business logc layer
```
8. What is @Repository?
```text
it's an annotation that is used to mark a class as a been but it's used in the data access layer
```

9. What is @Controller?
```text
it's a annotation that is used to mark a clas as a been for controller layer and it's ysed to handle the http requests
``` 
10. What is @RestController?
```text
it's a annotation that is used to mark a clas as a been for controller layer and it's used for the rest apis to return json data directly
```
11. Difference between @Controller and @RestController?
```text
@Controller is used to handle the http requests and return jsp or html response meanwhule @RestController used to retunr json
```
12. What is @Autowired?
```text
@Autowired is an annotation that is used to inject dependencies in a class.

``` 
13. What is constructor injection?
14. What is @Configuration?
```text
it's an annotation that is used to mark that this class is spring boot configuration and inside this class we can define  beans
```
15. What is @Bean?
```text
it's an annotaiton that it used to mark that this bean will be created when the pring boot app runs and store in thee sping continer orr ioc container
```
16. What is application.properties?
```text
it's a file that is used to configure the spring boot, it responsable to centralize the spring boot app configurations like database configuration and server port debuding options
```
17. What is Spring Boot starter?
```text
it's a dependency that is used to reduce the configuration of the spring boot, 
```
18. What is Spring Initializr?
```text
spring initializr is a web based tool that is used to create spring boot applications
```
19. What is actuator?
```text
spring actuator is a spring boot module that is used to monitor and manage the spring boot applications
```
20. What is profile in Spring Boot?
```text
spring profile is a way to define different profiles for the spring boot applications
```
21. What is @Value?
```text
it's an annotation that is used to inject the values from the application.properties file to the java class
```
22. What is @ConfigurationProperties?
```text
it's an annotation that is used to inject the values from the application.properties file to the java class in a typed way
```
23. What is DTO?
```text
it's a data transfer object that is used to transfer the data between the client and the server
```
24. Why use DTOs?
```text
it's used to transfer the data between the client and the server and it's more efficient than using the entity objects.
```
25. What is validation?
```text
it's a process of validating the data that is sent from the client to the server
```
26. What is @Valid?
```text
it's an annotation that is used to validate the data that is sent from the client to the server
```
27. What is @RequestBody?
```text
it's an annotation that is used to bind the HTTP request body to a Java object
```
28. What is @PathVariable?
```text
it's an annotation that is used to bind the URL path variables to a Java object
```
29. What is @RequestParam?
```text
it's an annotation that is used to bind the URL query parameters to a Java object
```
30. What is ResponseEntity?
```text
it's an annotation that is used to return the HTTP response to the client
```
31. What is global exception handling?
```text
it's a way to handle the exceptions in a centralized manner
```
32. What is @ControllerAdvice?
```text
it's an annotation that is used to handle the exceptions in a centralized manner
```
33. What is @ExceptionHandler?
```text
it's an annotation that is used to handle the exceptions in a centralized manner
```
34. What is Spring Data JPA?
```text
spring data jpa is a spring boot module that is used to handle the data access layer
```
35. What is JpaRepository?
```text
jpaRepository is an interface that is used to handle the data access layer
```
36. Difference between PUT and PATCH?
```text
put is used to update the entire resource and patch is used to update the partial resource
```
37. What is pagination?
```text
pagination is a way to divide the data into pages
```
38. What is sorting in Spring Data?
```text
sorting is a way to sort the data in a specific order
```
39. What is lazy loading?
```text
it's type of fetch data, it's fetch only the main data and the related data fetch on demand
```
40. What is eager loading?
```text
it's type of fetch data, it's fetch all the data including the related data
```

41. What is transaction?
```text
it's a group of operations that are executed together as a single unit
```
42. What is @Transactional?
```text
it's an annotation taht it used to create a transaction to db and if it fails rollback
```
43. What is bean scope?
```text
scope is how the spring boot manage beans in each request.
```
44. Singleton vs prototype scope?
```text
singleton: only one bean created one for all requests in program lifecycle
prototype: a new bean created each request
```
45. What is circular dependency?
```text
it's a case when two class are dependent on each other
```
46. What is CORS?
```text
cross origin resource sharing allowes the web browser to consume apis from different domains, and reject other domains to consume our app resources
```
47. How to secure APIs?
```text
to secure api we use jwt, authentication, authorization, dtos
```
48. What is interceptor?
```text
interceptor is a spring mvc concept that is used to intercept the requests before sending to the controller
```
49. What is filter?
```text
filter is a servlet concept that is used to intercept the requests before sending to the controller
```
50. Difference between filter and interceptor?
```text
filter is a servlet concept that is used to intercept the requests before sending to the controller
interceptor is a spring mvc concept that is used to intercept the requests before sending to the controller
```

---

# 8. Spring Security

1. What is Spring Security?
```text
it's framework that used to secure our app by handling authentication, authorization and other security features like session management, jwt token, and resource sharing.
```

2. Authentication vs Authorization?
```text
 - Authentication is the process of verifying the identity of the user
 - Authorixation is the process of verifying the user has role to certain resource or not
 ```
3. What is JWT?
```text
JWT: it's stands for json web token, and it's stateless, it's used for authentication and authorization
```
4. What is token?
```text
tokenn is string contains all user credentials and the role, and it's encoded by our secret key.
```
5. What is BCrypt?
```text
it's a strong one direction hashing algorithmm that used to hash and verify the user password
```
6. What is UserDetailsService?
```text
it's an interface that is used to load the user details from database
```
7. What is SecurityContext?
```text
it's an object that is used to store the security context of the user in other words: that object contains the authenticated user details like username, roles, etc
```
8. What is SecurityFilterChain?
```text
it's a configuration that configures the security features like authentication, authorization, etc
```
9. What is password encoding?
```text
it's a proccess to change the data to unreadable data to protect it called encoding and convert it to readable data is called decoding and it's used to encode and decode the password.
```
10. What is role-based authorization?
```text
it's a authorization that is based on the role of the user
```
11. What is OAuth2?
```text

```
12. What is CSRF?
13. What is CORS?
14. Session authentication vs JWT?
15. What is stateless authentication?
16. What is refresh token?
17. What is access token?
18. What is Principal?
19. What is GrantedAuthority?
20. What is DelegatingFilterProxy?

---

# 9. Hibernate & JPA

1. What is Hibernate?
```text
hibernate is an aimplementation of jpa and it's provide high performance of data access layer
```
2. What is JPA?
```text
jpa is a spesification of orm in java

```

3. Difference between Hibernate and JPA?
```text
JPA is a specification that dfine the rule of orm in java.
Hibernate is the famous implementation of jpa 
```
4. What is ORM?
```text
ORM: it stands for Object Retional mapping
it's technique used to map java ojbjects as tables in database, and convert data in the database to java object automaticaly
```
5. What is entity?
```text
it's java class that present table in database.
```
6. What is @Entity?
```text
it's an annotation used to tell the spring data jpa(ibernate) that this class is an entity class, so the hibernate map that objects to database tables
```
example:
```java
@Entity
@Table(name = "users")
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    private String name;
    private String email;
}
```
7. What is @Table?
```text
it's an annotation used to define that class map to table in database, so hibernate  responsable to create that table in db automatically.
```

8. What is @Id?
```text
it's an annotation used to define that the field is the primary key of the entity
```

9. What is @GeneratedValue?
```text
it's an annotation used to define that the field is auto generated
there are many types of auto generation:
  - AUTO: hibernate choose the best based on the db;
  - IDENTITY: identity is used for mysql db
  - SEQUENCE: sequence is used for postgresql db and oracle db
  - TABLE: table is used for sql server db
```
10. What is persistence context?
```text
it's a cache that is used to store the entities
```
11. What is EntityManager?
```text
it's an interface that is used to manage the entity
```
12. What is dirty checking?
```text
dirty checking is the process of detecting the changes in the entity
```
13. What is first-level cache?
```text
it's a cache that is used to store the entities
```
14. What is second-level cache?
```text
it's a cache that is used to store the entities
```
15. What is N+1 problem?
```text
it's a problem that occurs when the hibernate fetch the data from the database
```
16. What is cascade type?
```text
it's a type that is used to define the cascade type
```
17. What is orphanRemoval?
```text
it's a type that is used to define the orphan removal
```
18. What is @OneToMany?
```text
it's an annotation used to define the one-to-many relationship
```
19. What is @ManyToOne?
```text
it's an annotation used to define the many-to-one relationship
```
20. What is @ManyToMany?
```text
it's an annotation used to define the many-to-many relationship
```
21. What is @JoinColumn?
```text
it's an annotation used to define the join column
```
22. What is fetch type?
```text
it's a type that is used to define the fetch type
```
23. Lazy vs eager fetch?
```text
it's a type that is used to define the fetch type
```
24. What is JPQL?
```text
it's an object-oriented query language
```
25. Native query vs JPQL?
```text
it's an sql query
```

---

# 10. REST API Questions

1. What is REST?
2. What are REST principles?
3. What is stateless API?
4. GET vs POST?
5. PUT vs PATCH?
6. DELETE request?
7. What is idempotency?
8. What are HTTP status codes?
9. What is 200?
```text
it's a status code that is used to indicate that the request was successful
```
10. What is 201?
```text
it's a status code that is used to indicate that the request is created
```
11. What is 400?
```text
it's a status code that is used to indicate that the request was failed due to server error
```
12. What is 401?
```text
it's a status code that is used to indicate that the request was unauthorized
```
13. What is 403?
```text
it's a status code that is used to indicate that the request was forbidden
```
14. What is 404?
```text
it's a status code that is used to indicate that the request was not found
```
15. What is 500?
```text
it's a status code that is used to indicate that the request was failed due to server error
```
16. What is API versioning?
17. What is pagination?
18. What is API gateway?
19. What is rate limiting?
20. What is Swagger/OpenAPI?

---

# 11. SQL & Database Questions

1. What is primary key?
```text
primary key is unique key that used to identify a row in the table like id or uuid
```
2. What is foreign key?
```text
foregin key is the key that reference to primary key in another table, and it used to create relationship between tables
```

3. What is unique constraint?
```text
unique constraint is used to ensure that all values in the column are unique.
```
4. What is normalization?
```text
normalization is the process of organizing the columns and tables 
```
5. What is denormalization?
```text
denormalization is the process of adding redundant data to the table to improve the performance.
```

6. What is index?
```text
index is a data structure that used to speed up the data searching from the database.
```
7. Clustered vs non-clustered index?
```text
clustered index is the index that used to store the data in the table in the same order as the index, and it is used to speed up the data searching from the database.
non-clustered index is the index that used to store the data in the table in a different order as the index, and it is used to speed up the data searching from the database.
```
8. What is JOIN?
```text
join is used to merge two tables or more to get the data match between them.
````
9. INNER JOIN?
```text
it's for getting all match data not null in the tables
```
10. LEFT JOIN?
```text
it's for getting all rows in the left table even if there is no match in the right table.
```
11. RIGHT JOIN?
```text
it's for getting all rows in the right table even there is no match in the left table like data is null.
12. FULL JOIN?
```text
it's retunr all match data for both tables 
```
13. What is GROUP BY?
```text
it's a clause to group the data together based on some condition
```
14. What is HAVING?
```text
it's a clause to filter the data that grouped by GROUP BY
```
15. Difference between WHERE and HAVING?
```text
WHERE is used to filter the data before grouping, while HAVING is used to filter the data after grouping
```
16. What is subquery?
```text
subquery is a query that is nested inside another query.
like select * from table where id in (select id from table2)
```
17. What is view?
```text 
is a 
```
18. What is transaction?
19. ACID properties?
```text
Atomicity = all or nothing
Consistency = valid data
Isolation = transactions separated
Durability = saved forever
```
20. What is deadlock?
21. What is database locking?
22. What is optimistic locking?
23. What is pessimistic locking?
24. What is SQL injection?
25. How to prevent SQL injection?
26. What is composite key?
27. What is auto increment?
28. What is cardinality?
29. What is schema?
30. Difference between SQL and NoSQL?

---

# 12. Microservices

1. What are microservices?
2. Microservices vs monolith?
3. Advantages of microservices?
4. Challenges of microservices?
5. What is service discovery?
6. What is Eureka server?
7. What is API gateway?
8. What is load balancing?
9. What is circuit breaker?
10. What is distributed tracing?
11. What is centralized logging?
12. What is message broker?
13. Kafka vs RabbitMQ?
14. What is event-driven architecture?
15. What is eventual consistency?
16. What is distributed transaction?
17. What is Saga pattern?
18. What is Docker?
19. What is Kubernetes?
20. What is containerization?
21. What is scaling?
22. Horizontal vs vertical scaling?
23. What is fault tolerance?
24. What is resilience?
25. What is observability?

---

# 13. System Design

1. Design URL shortener.
2. Design chat application.
3. Design appointment booking system.
4. Design banking transaction system.
5. Design notification system.
6. Design file upload system.
7. Design authentication system.
8. What is caching?
9. Redis use cases?
10. What is CDN?
11. What is database sharding?
12. What is replication?
13. What is CAP theorem?
14. What is consistency?
15. What is availability?
16. What is partition tolerance?
17. What is load balancer?
18. What is reverse proxy?
19. What is horizontal scaling?
20. What is vertical scaling?
21. What is rate limiter?
22. What is queue?
23. What is asynchronous communication?
24. What is WebSocket?
25. What is polling?

---

# 14. Design Patterns

## Creational Patterns

1. Singleton Pattern
2. Factory Pattern
3. Abstract Factory Pattern
4. Builder Pattern
5. Prototype Pattern

## Structural Patterns

6. Adapter Pattern
7. Decorator Pattern
8. Facade Pattern
9. Proxy Pattern
10. Composite Pattern

## Behavioral Patterns

11. Observer Pattern
12. Strategy Pattern
13. Command Pattern
14. State Pattern
15. Template Method Pattern
16. Iterator Pattern
17. Mediator Pattern
18. Chain of Responsibility
19. Visitor Pattern
20. MVC Pattern

---

# 15. Coding Patterns

## 1. Two Pointers

Common Problems:

* Reverse string
* Palindrome
* Sorted two sum
* Remove duplicates

### Reverse String

## Brute Force

```java
String reverse = "";

for (int i = str.length() - 1; i >= 0; i--) {
    reverse += str.charAt(i);
}
```

Time Complexity: O(n²)

## Better Solution Using StringBuilder

```java
String reversed = new StringBuilder(str).reverse().toString();
```

Time Complexity: O(n)

## Optimal Two Pointer Solution

```java
char[] chars = str.toCharArray();

int left = 0;
int right = chars.length - 1;

while (left < right) {
    char temp = chars[left];
    chars[left] = chars[right];
    chars[right] = temp;

    left++;
    right--;
}

return new String(chars);
```

Time Complexity: O(n)
Space Complexity: O(1)

---

## 2. HashMap Pattern

Common Problems:

* Two Sum
* Frequency Counter
* Duplicate Detection
* Group Anagrams

### Two Sum

## Brute Force

```java
for (int i = 0; i < nums.length; i++) {
    for (int j = i + 1; j < nums.length; j++) {
        if (nums[i] + nums[j] == target) {
            return new int[]{i, j};
        }
    }
}
```

Time Complexity: O(n²)

## Optimal HashMap Solution

```java
Map<Integer, Integer> map = new HashMap<>();

for (int i = 0; i < nums.length; i++) {

    int complement = target - nums[i];

    if (map.containsKey(complement)) {
        return new int[]{map.get(complement), i};
    }

    map.put(nums[i], i);
}
```

Time Complexity: O(n)

---

## 3. Sliding Window Pattern

Common Problems:

* Longest substring without repeating characters
* Maximum sum subarray
* Minimum window substring

### Maximum Sum Subarray of Size K

## Brute Force

```java
int max = Integer.MIN_VALUE;

for (int i = 0; i <= nums.length - k; i++) {

    int sum = 0;

    for (int j = i; j < i + k; j++) {
        sum += nums[j];
    }

    max = Math.max(max, sum);
}
```

Time Complexity: O(n*k)

## Sliding Window Solution

```java
int windowSum = 0;

for (int i = 0; i < k; i++) {
    windowSum += nums[i];
}

int max = windowSum;

for (int i = k; i < nums.length; i++) {

    windowSum += nums[i] - nums[i - k];

    max = Math.max(max, windowSum);
}
```

Time Complexity: O(n)

---

## 4. Stack Pattern

Common Problems:

* Valid parentheses
* Next greater element
* Undo operations

### Valid Parentheses

```java
Stack<Character> stack = new Stack<>();

for (char c : s.toCharArray()) {

    if (c == '(' || c == '{' || c == '[') {
        stack.push(c);
    } else {

        if (stack.isEmpty()) {
            return false;
        }

        char top = stack.pop();

        if (
            (c == ')' && top != '(') ||
            (c == '}' && top != '{') ||
            (c == ']' && top != '[')
        ) {
            return false;
        }
    }
}

return stack.isEmpty();
```

---



## 5. BFS / DFS Pattern

### DFS Recursive

```java
void dfs(TreeNode node) {

    if (node == null) {
        return;
    }

    dfs(node.left);
    dfs(node.right);
}
```

### BFS

```java
Queue<TreeNode> queue = new LinkedList<>();

queue.offer(root);

while (!queue.isEmpty()) {

    TreeNode current = queue.poll();

    if (current.left != null) {
        queue.offer(current.left);
    }

    if (current.right != null) {
        queue.offer(current.right);
    }
}
```

---

# 16. Top Coding Problems

## Easy


1. Two Sum
2. Valid Parentheses
3. Reverse String
4. Palindrome Number
5. Valid Anagram
6. Merge Sorted Array
7. Binary Search
8. Remove Duplicates
9. Maximum Subarray
10. Contains Duplicate


## Medium

11. Longest Substring Without Repeating Characters
12. Group Anagrams
13. Product of Array Except Self
14. Merge Intervals
15. Number of Islands
16. Rotting Oranges
17. Top K Frequent Elements
18. Search in Rotated Sorted Array
19. Coin Change
20. Word Break

## Hard

21. LRU Cache
22. Median of Two Sorted Arrays
23. Trapping Rain Water
24. Serialize and Deserialize Binary Tree
25. Merge K Sorted Lists

---


# 17. Behavioral Questions

1. Tell me about yourself.
2. Why do you want to work at Citi?
3. Tell me about a difficult bug.
4. Tell me about conflict in a team.
5. Tell me about a deadline challenge.
6. Describe a project you are proud of.
7. How do you learn new technologies?
8. Tell me about failure.
9. Tell me about leadership.
10. Why should we hire you?
11. Describe your biggest strength.
12. Describe your biggest weakness.
13. Tell me about a stressful situation.
14. Describe a time you solved a difficult problem.
15. Describe teamwork experience.

---




# 18. HR Questions

1. Expected salary?
2. Are you open to relocation?
3. Why are you leaving current role?
4. What are your career goals?
5. Where do you see yourself in 5 years?
6. Why backend development?
7. Why Java?
8. Why Spring Boot?
9. What motivates you?
10. What makes you different?

---

# 19. Final Interview Tips

## During Coding

1. Clarify the problem first.
2. Ask about edge cases.
3. Explain brute force first.
4. Then optimize.
5. Talk while coding.
6. Test manually.
7. Mention complexity.
8. Use clean variable names.
9. Stay calm.
10. Debug out loud.

---

# Final Advice

Master these topics:

* HashMap
* Two Pointers
* Sliding Window
* Stack
* BFS/DFS
* Spring Boot
* SQL
* REST APIs
* OOP
* System Design Basics
